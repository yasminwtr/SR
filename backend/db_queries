CREATE DATABASE application_database

CREATE TABLE person(
    idPerson SERIAL PRIMARY KEY,
    email VARCHAR(45) NOT NULL,
    pass VARCHAR(45) NOT NULL,
    fullName VARCHAR(45) NOT NULL,
    phoneNumber VARCHAR(45) NOT NULL
);

CREATE TABLE service(
    idService SERIAL PRIMARY KEY,
    titleService VARCHAR(45),
    icon VARCHAR(300)
);

CREATE TABLE worker(
    idWorker SERIAL PRIMARY KEY,
    idPerson INT NOT NULL,
    idService INT NOT NULL,
    descriptionService VARCHAR(100),
    localization VARCHAR(100),
    priceService NUMERIC,
    FOREIGN KEY (idPerson) REFERENCES person (idPerson),
    FOREIGN KEY (idService) REFERENCES service (idService)
);

SELECT
  idWorker,
  worker.idPerson,
  person.idPerson,
  fullname,
  phoneNumber,
  idService,
  descriptionService,
  localization
FROM worker
INNER JOIN person
ON person.idPerson = worker.idPerson
WHERE worker.idService = 1

insert into service(titleservice, icon) values ('Mecânico','https://cdn-icons-png.flaticon.com/512/2555/2555013.png') 
insert into service(titleservice, icon) values ('Programador','https://cdn-icons-png.flaticon.com/512/711/711284.png')
insert into service(titleservice, icon) values ('Jardinagem','https://cdn-icons-png.flaticon.com/512/1518/1518965.png')
insert into service(titleservice, icon) values ('Manicure','https://cdn-icons.flaticon.com/png/512/1658/premium/1658273.png?token=exp=1657588718~hmac=538ae2973f424bd11730be803c36c80f')
insert into service(titleservice, icon) values ('Diarista','https://cdn-icons.flaticon.com/png/512/2954/premium/2954891.png?token=exp=1657589237~hmac=3aba855ca707bb745a514dadf8229596') 